// ES6 Modules = An external file that contain reusable code that can be imported into other Javascript files.
//               Write reusable code for many different apps
//               Can contain variables, classes, functions ... and more
//               Introduced as ECMAScript 2015 updates

import { circumference } from "./mathUtil.js";
import { area } from "./mathUtil.js";
import { volume } from "./mathUtil.js";
import { PI } from "./mathUtil.js";

console.log(PI);
console.log(circumference(3));
console.log(area(3));
console.log(volume(3));
